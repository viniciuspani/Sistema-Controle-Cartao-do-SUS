CREATE DATABASE db_CadSus
ON PRIMARY
(NAME = db_CadSus,
FILENAME = 'C:\BANCO TESTES\db_CadSus.MDF',
SIZE = 10MB,
MAXSIZE = 40MB,
FILEGROWTH = 10%
)

USE db_CadSus
DROP TABLE tbl_Operador

CREATE TABLE tbl_Operador
(id INT PRIMARY KEY IDENTITY (1,1),
unidade VARCHAR(100) NOT NULL,
operador VARCHAR (200) NOT NULL,
cnes INT NOT NULL,
cpf VARCHAR (15) NOT NULL,
senha VARCHAR(50) NOT NULL,
nivel VARCHAR(25) NOT NULL
)

BULK
INSERT tbl_Operador
FROM 'E:\BANCO TESTE\tblOperadorOriginal.csv'
WITH
( FIRSTROW = 2,
FIELDTERMINATOR = ';',
ROWTERMINATOR = '\n',
TABLOCK)